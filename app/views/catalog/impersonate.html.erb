<h1>User Impersonation</h1>

<p>
 This is an administrator utility that allows a admin to impersonate another user in the system for support purposes.
</p>

<p>Search for a user to impersonate:</p>

<%= form_tag root_url, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>

<% if @users %>
<% if @users.empty? %>
<p>No users found</p>
<% else %>
<ul>
  <% @users.each do |user| %>
  <li>
    <%= user %>
    <%= link_to "Impersonate", impersonate_engine.impersonate_user_path(user) %>
  </li>
  <% end %>
</ul>
<% end %>
<% end %>